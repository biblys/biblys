<div class="OverallMenu overall-menu">
  <a href="/pages/cart">
    <span class="fa fa-shopping-basket"></span>
    Mon panier
  </a>

  {% if not app.currentUser.isAuthentified %}
    <a class="OverallMenu__entry" href="{{ path("user_login", { return_url: app.currentUrl.relativeUrl }) }}">
      <span class="fa fa-user-circle-o"></span>
      Connexion
    </a>
  {% else %}
    <a class="OverallMenu__entry" href="{{ path("user_account") }}">
      <span class="fa fa-user-circle"></span>
      Mon compte
    </a>
  {% endif %}
</div>
