{{ form_start(form) }}
    <fieldset>
      {{ form_widget(form) }}
    </fieldset>

    <fieldset class="text-center">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    </fieldset>
{{ form_end(form) }}
