<h1>Abonnement Biblys Cloud</h1>

<p>Date d'expiration : {{ cloud_expiration_date|date("j f Y") }}</p>
{% if domains %}
  <p>Domaines inclus : {{ domains|join(", ") }}</p>
{% endif %}

<a href="{{ path('main_admin_cloud_portal') }}" class="btn btn-primary">
  <span class="fa fa-cloud"></span>
  &nbsp;
  Gérer mon abonnement
</a>
