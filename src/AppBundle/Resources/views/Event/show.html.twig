{% extends "layout:base.html.twig" %}

{% block title %}
  {{ event.title }}
{% endblock %}

{% block main %}
  {% include 'AppBundle:Event:_event.html.twig' %}

  {% if app.user.isAdmin and events is not defined %}
    <div class="admin">
      <p>Evènement n°
        {{ event.id }}</p>
      <p>
        <a href="/pages/event_edit?id={{ event.id }}">
          modifier
        </a>
      </p>
    </div>
  {% endif %}
{% endblock %}
